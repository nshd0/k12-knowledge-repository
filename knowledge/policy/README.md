# Policy

This folder contains MoE circulars, NEP 2020 frameworks, NCF 2023 documents, CBSE notifications, and DoE guidelines.

## Rules
- All policy files require **human approval** before indexing (see `governance/safety_rules.yaml`).
- `content_type` must be set to `policy`.
- `review_status` must be `approved` before ingestion.
