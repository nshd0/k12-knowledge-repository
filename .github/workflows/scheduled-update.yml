# ARCHIVED — superseded by weekly-update.yml
# This workflow was an earlier, weaker version of the weekly scrape+commit job.
# weekly-update.yml runs the same scripts with dry_run support, a proper commit
# guard, and an update_log entry. Do not re-enable this file.
#
# To trigger a manual refresh use:
#   Actions → Weekly — Repository Data Refresh → Run workflow

# name: Scheduled Repository Update   ← disabled
# on: ~
