# ARCHIVED — the Netlify deploy hook is replaced by GitHub Pages.
# pages-deploy.yml deploys index.html on every push to main with no
# external service, no secret required, and no Netlify account dependency.
#
# If Netlify is re-adopted as the hosting platform:
#   1. Add NETLIFY_DEPLOY_HOOK secret in repo settings
#   2. Restore the job below and delete pages-deploy.yml

# name: Netlify — Deploy Trigger   ← disabled
# on: ~
