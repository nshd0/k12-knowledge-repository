# ARCHIVED — scraper/orchestrator.py is now called inside weekly-update.yml
# (step: "Run source ingestion" → python scripts/ingest_sources.py)
# The scraper/data_validator.py validation is also covered by data-integrity.yml.
#
# The separate Sunday-at-8:30pm-UTC cron created a second concurrent weekly run
# that raced with the Monday-3:30am-UTC run in weekly-update.yml.
# Both ran the same scripts on the same data directory with no coordination.
#
# If you need to re-run the scraper in isolation:
#   Actions → Weekly — Repository Data Refresh → Run workflow (dry_run=false)

# name: Run Data Scraping   ← disabled
# on: ~
