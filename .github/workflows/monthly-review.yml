# ARCHIVED — URL health check merged into data-integrity.yml.
# data-integrity.yml now runs on every push to data/ AND on the 1st of each
# month via its own schedule trigger, covering both the per-commit CSV check
# and the monthly source URL verification.
#
# The source_health.json output and docs/update_log.md entry are preserved
# in the merged integrity workflow.

# name: Monthly — Deep Source Review   ← disabled
# on: ~
