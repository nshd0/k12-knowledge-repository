# Assessments

This folder contains questions, rubrics, and answer keys for all K12 subjects.

## Structure
- `grade{N}/` - Assessments by grade
- `{subject}/` - By subject within grade

## Frontmatter Requirements
All assessment files must include full metadata per `metadata/schema.yaml`.
`content_type` must be set to `assessment`.
