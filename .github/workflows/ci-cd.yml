# ARCHIVED — the Vite/Netlify build is no longer the primary deploy path.
# GitHub Pages (pages-deploy.yml) now serves index.html directly as a
# static site without a build step, making this CI job unnecessary.
#
# If the Vite/React app in /src/ is revived as the primary frontend,
# restore this workflow and update pages-deploy.yml to point at dist/ instead.
#
# The CSV validation step from this file has been absorbed into data-integrity.yml.

# name: CI — Build & Validate   ← disabled
# on: ~
